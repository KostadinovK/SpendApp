{{>header}}
<section class="cta">
    {{#if loggedIn}}
    <h2>Welcome back, <span class="username">{{username}}</span></h2>
    <a href="#/incomes/register">Register Income</a>
    <a href="#/payments/register">Register Payment</a>
    {{else}}
    <h2>Saving More For Things That Matter</h2>
    <h3>We Made Financial Management Easy</h3>
    <a href="#/register">Create Account Now</a>
    {{/if}}
</section>
{{>footer}}